import { type Task, type Meeting } from '@/lib/types';

// This file can be used to centralize data-related logic and definitions.
// It previously held mock data, which has been removed as the app now
// uses live data from Firebase Firestore.

export const initialTasks: Task[] = [
 
];

export const initialMeetings: Meeting[] = [
   
];
